# BMI-Calculator
A Python project that takes calculates the user's Body Mass Index (BMI) and categorizes it according to standard BMI Classifications. It features interactive user input, BMI categorizations, and the BMI Formula: (weight/height^2)*703. It uses conditional statements, arithmetic operations, and input/output handling.
